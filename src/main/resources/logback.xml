<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOGSTASH" class="ch.qos.logback.classic.net.SocketAppender">
        <remoteHost>${LOGSTASH_HOST:-logstash}</remoteHost>
        <port>${LOGSTASH_PORT:-5046}</port>
        <includeCallerData>true</includeCallerData>
    </appender>

    <logger name="com.mchange.v2" level="warn"/>
    <logger name="io.netty" level="warn"/>
    <logger name="io.vertx" level="info"/>
    <logger name="com.hazelcast" level="info"/>
    <logger name="io.piveau" level="info"/>

    <logger name="pipe" level="${PIVEAU_PIPE_LOG_LEVEL:-INFO}">
        <appender-ref ref="LOGSTASH" />
    </logger>

    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>
